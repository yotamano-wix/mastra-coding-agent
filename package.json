{
  "name": "mastra-coding-agent",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend-ts",
    "build": "npm run build --prefix backend-ts",
    "start": "npm run start --prefix backend-ts"
  }
}
